{
  "name": "odtuvt-site",
  "version": "1.0.0",
  "description": "ODTÜ Verimlilik Topluluğu web sitesi — Apple tasarım diliyle yeniden tasarım",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "yayinla": "git add -A && (git commit -m \"içerik güncellendi\" || cd .) && git pull --no-edit && git push",
    "guncelle": "git add -A && (git commit -m \"yerel içerik\" || cd .) && git pull --no-edit"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "ejs": "^3.1.10",
    "express": "^4.21.2"
  },
  "license": "MIT"
}
